class Requests:
    def __init__(self, _id, user_id, forniture_list, floor_layout):
        self._id = _id
        self.user_id = user_id
        self.forniture_list = forniture_list
        self.floor_layout = floor_layout
